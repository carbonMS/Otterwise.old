// Crie um arquivo chamado helpers.js e nele crie uma função que soma 2 números qualquer.
// Faça o export dessa função e em outro arquivo chamado index.js importe a função e a execute.

export const addUp = (num1, num2) => {
  console.log(num1 + num2)
}
